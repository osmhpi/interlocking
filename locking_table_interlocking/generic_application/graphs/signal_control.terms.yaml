entity_type: Signal
terms:
  RouteOpening:
    expression: Any(route in @routes_starting_here | RouteMonitoring[route].State == ActiveInactive::ACTIVE)
    default: false
variables:
  State:
    type: OpenCloseState
    description: Indicates whether the signal is open or closed.
    default: OpenCloseState::CLOSED
