entity_type: Point
terms:
  EndPositionLeftDetected:
    expression: SCIP.DetectedEndPosition == EulynxEndPosition::LEFT
    default: false
  EndPositionRightDetected:
    expression: SCIP.DetectedEndPosition == EulynxEndPosition::RIGHT
    default: false
  CommandedLeft:
    expression: PointNominalPosition.State == PointState::LEFT
    default: false
  CommandedRight:
    expression: PointNominalPosition.State == PointState::RIGHT
    default: false
variables:
  Left:
    type: ActiveInactive
    description: Indicates whether the point is monitored at left end position.
    default: ActiveInactive::INACTIVE
  Right:
    type: ActiveInactive
    description: Indicates whether the point is monitored at right end position.
    default: ActiveInactive::INACTIVE
