entity_type: Signal
terms:
  RouteOpening:
    expression: Any(route in @routes_starting_here | RouteControl[route].State == OpenCloseState::OPEN)
    default: false
variables:
  State:
    type: OpenCloseState
    description: Indicates whether the signal is open or closed.
    default: OpenCloseState::CLOSED
