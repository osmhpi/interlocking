entity_type: Transit
terms:
  RouteMonitored:
    expression: RouteMonitoring.State == ActiveInactive::ACTIVE
    default: false
variables:
  State:
    type: OpenCloseState
    description: Indicates whether the route is open or closed.
    default: OpenCloseState::CLOSED
